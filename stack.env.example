# =============================================================================
# CyberGAP - Variables de Entorno para Portainer Stack
# =============================================================================
# 
# CÓMO USAR EN PORTAINER:
# 1. Stacks → Add stack → Repository
# 2. En "Environment variables", agregar cada línea como variable
# 3. O cargar este archivo con "Load variables from .env file"
#
# IMPORTANTE: Cambiar TODOS los valores antes de desplegar
# =============================================================================

# -----------------------------------------------------------------------------
# SEGURIDAD (OBLIGATORIO CAMBIAR)
# -----------------------------------------------------------------------------

# Clave secreta para JWT - Generar con: openssl rand -base64 32
SECRET_KEY=CAMBIAR_GENERAR_CON_openssl_rand_base64_32

# Clave encriptación SMTP - Generar con: openssl rand -base64 24 | head -c 32
# DEBE tener EXACTAMENTE 32 caracteres
ENCRYPTION_KEY=CAMBIAR_EXACTAMENTE_32_CARACTERE

# Password inicial del administrador
DEFAULT_ADMIN_PASSWORD=CambiarEstoAhora123!

# -----------------------------------------------------------------------------
# CONFIGURACIÓN DE RED
# -----------------------------------------------------------------------------

# URL base donde estará accesible el sistema
# Cambiar por la IP de tu Raspberry Pi o dominio
BASE_URL=http://192.168.1.100

# Puerto HTTP (cambiar si el 80 está ocupado)
PORT=80

# -----------------------------------------------------------------------------
# CONFIGURACIÓN REGIONAL
# -----------------------------------------------------------------------------

# Zona horaria
TZ=America/Santiago

# -----------------------------------------------------------------------------
# DESARROLLO (no cambiar en producción)
# -----------------------------------------------------------------------------

# Modo debug (false para producción)
DEBUG=false
